services:
  - type: web
    name: email-management-api
    runtime: python3
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:10000"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: PORT
        value: 10000
      - key: ENVIRONMENT
        value: production

  - type: pserv
    name: email-management-db
    runtime: postgres
    databaseName: email_management
    databaseUser: neondb_owner
    databasePassword: npg_rCb8uLfJHIU0
    databaseHost: ep-crimson-lake-ad0kbt5e-pooler.c-2.us-east-1.aws.neon.tech
    databasePort: 5432
    databaseUrlSecret: DATABASE_URL